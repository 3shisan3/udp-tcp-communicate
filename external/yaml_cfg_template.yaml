# 库使用的cfg配置模板

# 信号订阅目标，考虑到同类信号，但不同来源，需要分对象处理 (按举例格式编写即可)
subscribe_list:
  - ID: "1"
    IP: ""
    Port: 
  - ID: "2"
    IP: ""
    Port:

# 信号发送目标
send_list:
  - IP: ""
    Port: 
  - IP: ""
    Port:

# 管理指定信号发送频率
msg_manager_list:
  - name: "中文说明"            # 不做使用（中文跨平台易出问题），更多作为配置人员理解
    ID: "1"                     # 唯一标识
    send_interval: 2            # 频率（HZ）


# 其他配置(可选)
